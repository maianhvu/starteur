#question-card.card.full.flex
  = react_component 'QuestionBox', questions: [], answered: 0,
    questionsFetchUrl: @questions_url, answersPostUrl: @answers_url,
    feedbackUrl: "#{dashboard_index_path}#feedback}"
